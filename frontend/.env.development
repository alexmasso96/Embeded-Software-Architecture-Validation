# Dev token must match what the worker was started with:
#   ARCH_API_TOKEN=dev-token uvicorn backend.app:app --port 8765
# In production (Phase 3) pywebview injects window.__ARCH_TOKEN__ instead.
VITE_API_TOKEN=dev-token
