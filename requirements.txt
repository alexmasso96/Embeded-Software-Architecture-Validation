PyQt6>=6.6.0
pyelftools>=0.30
pandas>=2.1.0
openpyxl>=3.1.0
rapidfuzz>=3.0.0
PyInstaller>=6.0.0
capstone>=4.0.2
bcrypt>=4.0.0
requests>=2.31.0
cryptography>=42.0.0
cpp_demangle>=0.1.0
# Phase 1 (pywebview migration) — FastAPI worker process. PyQt6 above is dropped
# at Phase 4 cutover; these become the lasting runtime deps for the new app.
fastapi>=0.110
uvicorn[standard]>=0.29
sse-starlette>=2.0
httpx>=0.27
# Phase 3 — pywebview desktop shell (native window + file dialogs). On macOS this
# pulls pyobjc (WKWebView); on Windows it uses the system WebView2 runtime.
pywebview>=5.0
